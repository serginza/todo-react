import { createTheme } from '@mui/material';

export const TasksStatsTheme = createTheme({
  typography: {
    body1: {
      color: 'white',
      background: '#6c757d',
      margin: '0 5px',
      padding: '0 5px',
      borderRadius: '5px',
    },
  },
});
